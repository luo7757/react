// const requireApi = require.context('.', true, /.js$/)

// const obj = {};
// console.log(requireApi.keys())

// requireApi.keys().forEach((fileName, i) => {
//   if(fileName === "./index.js" || fileName === "./instance.js") return;
//   Object.assign(obj, requireApi(fileName));
// });

// export default obj;